services:
  - type: web
    name: backend
    env: node
    plan: free
    region: oregon
    buildCommand: 'npm install && npm run build'
    startCommand: 'npm start'
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: your-postgres-database
